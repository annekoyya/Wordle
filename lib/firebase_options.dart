// GENERATED FILE - PLACEHOLDER.
//
// This project ships without real Firebase credentials so it can be
// shared/zipped safely. To enable Firebase (cloud stats sync + the
// global leaderboard), run in the project root:
//
//   dart pub global activate flutterfire_cli
//   flutterfire configure
//
// That command overwrites this file with your own project's real
// DefaultFirebaseOptions. Until then, main.dart catches the
// initialization error and the app runs fine in local-only mode.
//
// See README.md -> "Firebase Setup" for the full walkthrough.

import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;
import 'package:flutter/foundation.dart' show defaultTargetPlatform, kIsWeb, TargetPlatform;

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'DefaultFirebaseOptions have not been configured for this project yet. '
      'Run `flutterfire configure` to generate real options, or ignore this '
      '(the app runs fine locally without Firebase).',
    );
  }
}
